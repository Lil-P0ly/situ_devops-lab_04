server {
        listen 80;
        listen [::]:80;

        root /var/www/{{ item }};
        index index.html index.htm index.nginx-debian.html inndexx;

        server_name {{ item }};

        location / {
                try_files $uri $uri/ =404;
        }
}